#pragma once

// draw debuging rectangles for boundaries
const bool c_debug_draw = true;

// kinematics
const float c_gravity_acc = 9.8f;       // N * kg^-1 (m * s^-2)
const float c_hmove_speed = 2.0f;		// m * s^-1
const float c_vmove_speed = 2.0f;		// m * s^-1
const float c_jump_speed = 6.0f;		// m * s^-1

// size constants
const float c_pixels_per_meter = 80.0f;

// maps
const int c_map_count = 3;

