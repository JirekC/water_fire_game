#pragma once


// kinematics
const float gravity_acc = -9.8f; 			// N * kg^-1 (m * s^-2)
const float hmove_speed = 2.0f;				// m * s^-1
const float vmove_speed = 2.0f;				// m * s^-1
const float jump_speed = 6.0f;				// m * s^-1

// size constants
const float pixels_per_meter = 80.0f;

